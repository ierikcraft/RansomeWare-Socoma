[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Socoma"
version = "0.1.0"
description = "Execute socoma from a library"
requires-python = ">=3.7"